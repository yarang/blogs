# Blog API 환경 설정

# API Keys (쉼표로 여러 키 구분)
# 키 생성: python -c "import secrets; print(f'blog_{secrets.token_urlsafe(32)}')"
BLOG_API_KEYS=blog_your_api_key_here

# 블로그 루트 경로 (기본값: ../)
BLOG_ROOT=/var/www/blog-repo

# 서버 포트
PORT=8000
